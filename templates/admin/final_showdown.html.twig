{% extends "base.html.twig" %}

{% block body %}
	<div id="container-admin" class="bg-dark">
		<h2>Admin Final ShowDown</h2>
		{{ form_start(formView) }}
		<br>
		{{ form_widget(formView) }}
		<br>
		<button type="submit" class="btn btn-success">
			Validate Showdown</button>
		{{ form_end(formView) }}
	</div>
{% endblock %}

{% block title %}
	Admin Final ShowDown
{% endblock %}
