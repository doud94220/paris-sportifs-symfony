{% extends "base.html.twig" %}

{% block body %}
	<h1>Let's bet on
		{{ round }}</h1>

	{{ form_start(formView) }}
	{{ form_errors(formView) }}

	{# {{ form_row(formView) }} #}
	{{ form_widget(formView) }}

	{{ form_end(formView) }}
{% endblock %}

{% block title %}
	titlllle
{% endblock %}
