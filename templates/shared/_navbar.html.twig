<nav id="strech" class="navbar navbar-expand-sm navbar-light bg-light">
	<ul class="navbar-nav">
		<li class="nav-item">
			<a id="home-link" class="nav-link" href="/">Home</a>
		</li>
		{% if app.user %}
			<li class="nav-item">
				<a href="{{ path('matchs_results_root') }}" class="nav-link">
					Matchs Results
				</a>
			</li>
			<li class="nav-item">
				<a href="{{ path('bet_root') }}" class="nav-link">
					Let's Bet
				</a>
			</li>
			<li class="nav-item">
				<a href="{{ path('show_ranking') }}" class="nav-link">
					Bettors Ranking
				</a>
			</li>
			<li class="nav-item">
				<a href="{{ path('show_all_tennis_players') }}" class="nav-link">
					Tennis Players
				</a>
			</li>
			{% if app.user.admin == 1 %}
				<li class="nav-item">
					<a href="{{ path('admin_home') }}" class="nav-link">Administration</a>
				</li>
			{% endif %}
			<li class="nav-item">
				<a href="{{ path('security_logout') }}" class="btn btn-danger">Logout</a>
			</li>
			<li id="centrer-le-surnom" class="nav-item">
				<span class="nav-link">
					<strong>Welcome
						{{ app.user.nickName }}
						!</strong>
				</span>
			</li>
		{% else %}
			<li class="nav-item">
				<a href="{{ path('user_creation') }}" class="nav-link">Inscription</a>
			</li>
			<li class="nav-item">
				<a href="{{ path('security_login') }}" class="btn btn-success">Login</a>
			</li>
		{% endif %}
	</ul>
</nav>
